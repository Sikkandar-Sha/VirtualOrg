# What the host-side scripts need: scripts/verify.py, scripts/score and the
# reference kit. This is deliberately not requirements.txt, which is the image's
# dependency set and pulls in an ASGI server and a Postgres driver the host does
# not run. CI installs exactly these two.
httpx==0.28.1
PyYAML==6.0.2
