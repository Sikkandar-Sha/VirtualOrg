## What this changes

<!-- One or two sentences. -->

## Checks

- [ ] `python3 scripts/verify.py` passes
- [ ] If a lens or the generator changed, I added or updated checks in `scripts/verify.py`
- [ ] If the generator changed, I have said so below, because it invalidates golden files

## Does this change the world?

<!-- Any change to the order of random draws in world/generate.py reshuffles the whole
     world, even when the change looks local. If so, say what generator_version becomes
     and which counts moved. If not, say "no generator change". -->
